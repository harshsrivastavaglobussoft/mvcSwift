# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DribbleInSwift' do
platform :ios, '8.0'
use_frameworks!
pod 'SDWebImage/GIF', '~> 4.0'

  # Pods for DribbleInSwift

  target 'DribbleInSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DribbleInSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
